# Render Blueprint — Docker만 사용 (루트 package.json 때문에 Node로 잡히는 문제 회피)
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: eternal-beam-api
    region: singapore
    plan: free
    dockerfilePath: ./Dockerfile
