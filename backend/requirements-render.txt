# Render 무료 플랜용 — pip 가볍게 (torch/ultralytics/modal 제외)
# 누끼(/api/cutout)·FFmpeg 프리뷰·합성에 충분합니다.
# Luma 강아지 전처리(YOLO)가 필요하면 나중에 ultralytics 를 추가하세요.

fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# Render 최신 Python에서는 2.0.57 휠이 없을 수 있음 → PyPI에 있는 최신 라인 사용
rembg>=2.0.68,<3
onnxruntime>=1.17.0
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
ffmpeg-python==0.2.0
supabase>=2.3.0
# 프로덕션 프리미엄 라우터의 Supabase JWT 검증 (backend/auth.py).
# 없으면 /api/v1/pet/premium/* 이 503 으로 닫힌다 — 인증 없이 열리지는 않는다.
PyJWT>=2.8.0
python-dotenv==1.0.1
requests>=2.31.0

# QR Shaker (Phase 10) 판매자/운영 QR 생성 서비스.
# 순수 파이썬·의존성 없음(76KB). QR 은 물리 제품에 인쇄되므로 직접 구현하지 않는다 —
# Reed-Solomon/마스킹이 미묘하게 틀린 QR 은 테스트에서는 읽히고 현장에서 실패한다.
segno>=1.6.0

# 인쇄 생산 파이프라인 (Phase 13). A5 편지 PDF + 85×55mm 카드.
# 한글은 reportlab 내장 CJK CID 폰트(HYSMyeongJo-Medium)로 렌더한다 —
# 폰트 바이너리를 저장소에 넣지 않아도 된다.
# ⚠️ CID 폰트는 **임베드되지 않는다**. 최종 인쇄 전 PRINT_LETTER_FONT_PATH 로
#    라이선스된 한글 TTF 를 지정해 임베드할 것 (아래 .env.example 참고).
reportlab>=4.0.0
