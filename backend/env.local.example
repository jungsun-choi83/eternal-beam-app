# Copy to env.local (same folder). Do not commit env.local.
# If AnySign4PC locks ".env", use this filename instead.

LUMA_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Optional: must match an existing Storage bucket name (default: user-assets)
# SUPABASE_STORAGE_BUCKET=user-assets
