[pytest]
testpaths = tests
# 통합 테스트(실제 SAM2/ViTMatte/YOLO 가중치 필요)는 기본 실행에서 제외한다.
# 가중치가 있는 로컬 머신에서는:  pytest backend/tests -m integration
addopts = -m "not integration"
markers =
    integration: 실제 모델 가중치가 필요한 테스트 (기본 실행에서 제외)
filterwarnings =
    ignore::DeprecationWarning
